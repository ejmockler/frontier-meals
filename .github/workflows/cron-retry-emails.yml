# Retry Failed Emails
#
# MIGRATED TO CLOUDFLARE WORKERS (Feb 2026)
# Now runs via frontier-meals-cron-frequent Worker every 6 hours.
# This workflow is kept for manual fallback only.

name: Retry Failed Emails

on:
  # schedule DISABLED - migrated to Cloudflare Workers
  # - cron: '0 */6 * * *'
  workflow_dispatch:

env:
  # Production domain - Cloudflare Pages with custom domain
  PRODUCTION_URL: 'https://frontiermeals.com'

jobs:
  retry-emails:
    name: Retry Failed Emails
    runs-on: ubuntu-latest
    steps:
      - name: Retry Failed Emails
        run: |
          response=$(curl -s -L -w "\n%{http_code}" -X POST "${{ env.PRODUCTION_URL }}/api/cron/retry-emails" \
            -H "Content-Type: application/json" \
            -H "Cron-Secret: ${{ secrets.CRON_SECRET }}")

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')

          echo "Response: $body"
          echo "HTTP Status: $http_code"

          if [ "$http_code" != "200" ]; then
            echo "::warning::Email retry returned status $http_code"
          fi
